apiVersion: v1
kind: Pod
metadata:
  name: affinity-test
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
  tolerations:
    - key: "app"
      operator: "Equal"
      value: "dxpro-applications"
      effect: "NoSchedule"
